// 1. Global Variable Test (POPI + PUSHI)
VAR global_var = 10
VAR result = global_var + 5

// 2. Local Variable Test (POPR + PUSHR)
FUNCTION TEST_OPTIMIZER(arg1)
    VAR local_var = arg1 * 2
    RETURN local_var // Should trigger DUP + POPR here
END_FUNCTION

TEST_OPTIMIZER(21)